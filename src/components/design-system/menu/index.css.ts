import { style } from '@vanilla-extract/css';

export const menu = style({
  margin: '0.8rem 0',
  cursor: 'pointer',
});

export const menuText = style({
  fontSize: '1.5rem',
  color: '#101010',
});
