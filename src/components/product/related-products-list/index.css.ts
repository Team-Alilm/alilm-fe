import { style } from '@vanilla-extract/css';

export const cardList = style({
  width: '60rem',
  display: 'flex',
  gap: '1.2rem',
  overflow: 'scroll',
});

export const cardWrapper = style({
  width: '20rem',
});
