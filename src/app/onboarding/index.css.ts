import { style } from '@vanilla-extract/css';

export const onboardingPage = style({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  width: '100%',
  height: '100%',
  padding: '32px',
  background: '#ff0000',
});
