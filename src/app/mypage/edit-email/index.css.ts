import { style } from '@vanilla-extract/css';

export const editEmail = style({
  width: '100%',
  height: '100%',
  padding: '0 1.6rem',
});

export const editEmailTitle = style({
  fontSize: '2rem',
  fontWeight: '600',
  margin: '2.4rem 0',
});
